# Jules Client Configuration
# Required: Jules API key for authentication
JULES_API_KEY=your_jules_api_key

# Required: Default repository for all sessions (repo/branch arguments are disabled)
JULES_DEFAULT_REPO=your_account/repo_name

# Required: Default branch for all sessions (defaults to "dev" if not set)
JULES_DEFAULT_BRANCH=dev